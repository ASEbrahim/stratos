# StratOS Scorer V2.1 Training — Comprehensive Report

**Date:** March 6, 2026
**Status:** Training in progress — step ~556/1188 (47%), ETA ~6.5 hours
**Purpose:** Share with another Claude instance for review of training approach

---

## CRITICAL DISCOVERY (from cross-Claude review)

**The V2 baseline numbers are NOT comparable.** The 0.393 MAE (contaminated eval) was measured via **direct PyTorch bf16 inference** during Phase 3 of training. The 1.533 MAE (holdout eval) was measured via **Ollama serving a Q8_0 GGUF with a potentially broken Modelfile** (with `/nothink`, `num_predict=2048`). These used completely different pipelines.

The 1.533 holdout number conflates FOUR variables:
1. **Generalization gap** — unseen articles vs memorized articles
2. **Quantization overhead** — bf16 → Q8_0 GGUF
3. **Format drift** — `/nothink` causing 41% parse failures, prompt changes
4. **Ollama serving layer** — different inference engine than training eval

**We have never measured holdout performance with a correct pipeline.** The true V2 holdout MAE (without format drift, through correct Ollama config) is unknown and could be anywhere from 0.7 to 1.5.

### Four measurements needed to isolate variables:
| Eval Set | Pipeline | Status | What It Measures |
|----------|----------|--------|-----------------|
| Contaminated | Direct PyTorch bf16 | **DONE: 0.393 MAE** | Memorization ceiling |
| Contaminated | Ollama Q8_0 + correct Modelfile | **NOT DONE** | Quantization + serving cost |
| Holdout | Direct PyTorch bf16 | **NOT DONE** | True generalization |
| Holdout | Ollama Q8_0 + correct Modelfile | **NOT DONE** (1.533 was with broken format) | Real-world deployment |

**Action item:** After V2.1 training finishes, re-run V2's original GGUF through Ollama with corrected Modelfile (no `/nothink`, `num_predict=256`) on holdout eval BEFORE comparing V2.1 results.

---

## 0. V2 Problems Diagnosed Before V2.1 Training

These are the specific issues found in the V2 scorer that motivated V2.1 changes:

### 0.1 — `/nothink` in Modelfile caused 41% parse failures
The V2 Modelfile included a `/nothink` directive to suppress Qwen3 think blocks. This caused the model to enter degenerate token loops, outputting slash-commands (`/soft`, `/nostart`, `/assistant`, `/nostart`...) instead of scores. **41% of outputs were unparseable.** Removing `/nothink` and letting Qwen3 produce natural empty think blocks `<think>\n</think>` dropped parse failures to ~5%. The think blocks are ~10 tokens of overhead and get stripped by the parser.

### 0.2 — Chinese output from Qwen3 (~7% parse failures)
The Qwen3 base model sometimes outputs Chinese score format: `力评分：3.5 | 原因：Article about...` instead of `SCORE: 3.5 | REASON: ...`. The V2 parser only matched the English format, causing ~7% additional parse failures. Combined with /nothink, total V2 parse failures reached 7.2% on holdout eval (108/1500 samples lost).

### 0.3 — Mid-range scoring collapse (moderate MAE 2.77, high MAE 2.56)
V2's worst performance was on scores 4.5-8.5 (moderate and high bands). The model had a strong tendency to **default to 8.5** for any article it found somewhat relevant, instead of discriminating between 5.0, 6.5, 7.0, 8.0, etc. Root cause: training data only contained short 1-sentence reasons (~154 chars) that didn't teach the model *why* articles fall in specific score ranges. The model learned "relevant = high score" without nuance.

### 0.4 — Training used only short `reason` field, discarding `think_text`
Opus scoring generates two fields:
- `reason`: Short 1-sentence summary (~154 chars avg) — e.g., *"Not relevant to petroleum engineering."*
- `think_text`: Full multi-paragraph reasoning (~1,133 chars avg) — e.g., *"Looking at the user's profile as a Petroleum Engineering student at Kuwait University, their interests center on reservoir simulation, drilling technology... The article discusses pediatric oncology advances at King Faisal Hospital. While both are in technical/scientific domains, there is zero overlap..."*

V2 training only used the short `reason`. The 7.3x longer `think_text` was generated, paid for, and discarded. This meant the model learned to mimic score labels without understanding the reasoning behind them.

### 0.5 — Eval contamination (same articles in train and eval)
The original `eval_v2.jsonl` used the same 813 articles as training (just different profiles). This inflated eval metrics: contaminated MAE was 0.789 vs true holdout MAE of 1.533. A proper holdout set with 50 completely unseen articles was created (`eval_holdout_v2.jsonl`), revealing the true performance gap.

### 0.6 — Only 30 profiles (limited diversity)
V2 trained on 30 profiles, mostly concentrated in Gulf/MENA region and technical fields. Missing common professions like teachers, truckers, dentists, pilots, electricians. This limited the model's ability to generalize across diverse user contexts.

### 0.7 — Post-training format drift
After V2 training, code changes to `scorer_adaptive.py` prompt format caused training/inference misalignment. The model saw different prompt structures at inference than it was trained on. This contributed to score drift (MAE degraded from 0.393 to 0.761 on contaminated eval) and 237 parse failures. Retraining fixed it, but demonstrated the fragility of format alignment.

### 0.8 — No keyword pre-filter (100% LLM scoring)
Every article went through the LLM scorer, including obvious noise (articles in Arabic about Egyptian soccer when the user is a Canadian nurse). No fast-path filtering existed. This wasted LLM inference time on ~50-60% of articles that could be trivially classified as noise by keyword overlap check.

### Summary: V2 Holdout Performance (what V2.1 must beat)
```
MAE:                 1.533   (target: <1.2, ideal: <0.8)
Direction accuracy:  92.3%   (target: >95%)
Spearman rho:        0.535   (target: >0.65)
Parse failures:      7.2%    (target: <2%)
Within 1 point:      53.1%   (target: >60%)
Within 2 points:     80.6%   (target: >85%)
Moderate band MAE:   2.77    (worst band — model defaults to 8.5)
High band MAE:       2.56    (second worst)
```

**CAVEAT:** These holdout numbers were measured through Ollama with a broken Modelfile (`/nothink`, `num_predict=2048`). The true V2 holdout performance with correct serving config is unknown and likely better. The 0.393 contaminated MAE was measured via direct PyTorch bf16 inference — a completely different pipeline. See Critical Discovery section at top.

---

## 1. What Is StratOS Scorer?

A fine-tuned LLM that scores news articles 0-10 for relevance to a specific user profile (role, location, interests, tracked companies). Used in production to filter ~300 articles per scan. Speed matters — a full scan should take <5 minutes, not 30+.

**Inference flow:** User profile → system prompt, article → user prompt → model outputs `SCORE: X.X | REASON: brief explanation`

---

## 2. Base Model

- **Qwen 3 8B** (`Qwen3ForCausalLM`)
- Hidden: 4096, layers: 36, heads: 32, KV heads: 8 (GQA), intermediate: 12288
- Vocab: 151,936, estimated ~8.2B parameters
- Starting from `v1_merged_base` — Qwen3 8B with V1 DoRA adapters already merged in
- Qwen3 produces `<think>...</think>` blocks by default (usually empty after fine-tuning, stripped at inference)

---

## 3. Training Configuration

```
Method:           DoRA (LoRA with magnitude decomposition)
Rank:             16
Alpha:            32
Dropout:          0.05
Target modules:   q_proj, k_proj, v_proj, o_proj, gate_proj, up_proj, down_proj
Batch size:       2
Gradient accum:   8 (effective batch = 16)
Learning rate:    1e-5, cosine schedule, 5% warmup
Epochs:           1
Max seq length:   1024 tokens
Loss:             completion_only (only assistant tokens contribute to loss)
Optimizer:        AdamW, bf16
Gradient ckpt:    Yes (use_reentrant=False)
Hardware:         AMD RX 7900 XTX (24GB VRAM), ROCm 6.2
Speed:            ~38.5s/step, ~12.5 hours total for 1188 steps
```

### Dual Weighting System
1. **WeightedRandomSampler** — oversamples rare score bands in each batch
2. **Per-sample loss weights** — multiplies each example's loss by its band weight

| Band | Score Range | Count | % of Data | Sampler Weight | Loss Weight |
|------|-----------|-------|-----------|---------------|-------------|
| noise | 0-2.5 | 17,255 | 90.8% | 0.5x | 0.5x |
| tangential | 2.5-4.0 | 1,125 | 5.9% | 1.0x | 1.0x |
| moderate | 4.5-6.5 | 344 | 1.8% | 1.5x | 1.5x |
| high | 7.0-8.0 | 175 | 0.9% | 2.0x | 2.0x |
| critical | 8.5-10.0 | 101 | 0.5% | 3.0x | 3.0x |

---

## 4. Training Data

### Source
- **39,071 total scored examples** in `scores_v2.json`
- 24,387 from **Claude Opus** via Anthropic Batch API (genuine reasoning)
- 14,684 from **Claude Code agents** (keyword-matching heuristics — see quality concern below)
- Capped at **19,000 training examples** (GPU memory limit)
- 813 unique articles × 47-49 profiles

### Score Distribution (training set)
```
Score  0-1:  5,599  █████████████████████████████████████████████████████████
Score  1-2:  9,512  ███████████████████████████████████████████████████████████████████████████████████████████████████
Score  2-3:  2,554  █████████████████████████
Score  3-4:    590  █████
Score  4-5:    138  █
Score  5-6:     92
Score  6-7:    239  ██
Score  7-8:    164  █
Score  8-9:     98
Score  9-10:    14
Average: 1.26
```

### Training Format (ChatML)
```
SYSTEM: You are a relevance scorer for a {role} in {location}.
User context: {context}
Tracked companies: {companies}
Tracked institutions: {institutions}
Tracked interests: {interests}
Tracked industries: {industries}
{level_note: student vs professional}

Score each article 0.0-10.0:
9-10: Directly actionable (hiring match, breakthrough in tracked area)
7-8.9: Highly relevant to user's field/interests
5-6.9: Somewhat relevant
0-4.9: Not relevant / noise / wrong level

Reply ONLY with: SCORE: X.X | REASON: brief explanation

USER: Score this article:
Category: general
Keywords: {profile interests}
Title: {article title}
Content: {article content, max 500 chars}

ASSISTANT: SCORE: X.X | REASON: {think_text or short reason}
```

### 47 Profiles in Training
30 original Opus-scored profiles + 17 agent-scored profiles covering diverse professions worldwide: engineers, students, chefs, filmmakers, doctors, pilots, truckers, veterinarians, journalists, etc. across Kuwait, USA, UK, Japan, Brazil, Turkey, Germany, and more.

---

## 5. Key V2.1 Change: Think_text in Training

### What changed
- **V2 (before):** Assistant message used short `reason` field (~154 chars avg)
  - Example: `SCORE: 1.0 | REASON: Article about pediatric cancer is unrelated to petroleum engineering interests.`
- **V2.1 (now):** Assistant message uses full `think_text` field (~843 chars avg) when available
  - Example: `SCORE: 1.0 | REASON: Looking at this user's profile, they are a Petroleum Engineering student at Kuwait University with interests in reservoir simulation, drilling tech, and oil/gas careers. The article discusses pediatric oncology treatment advances at King Faisal Hospital in Riyadh. While both are in technical/scientific domains, there is zero overlap between oncology research and petroleum engineering. The user tracks companies like KOC, SLB, and Halliburton — none of which are mentioned. The geographic relevance to the Gulf region is the only tangential connection, but that alone does not make an oncology article useful to a petroleum engineering student.`

### Statistics
- 92.2% of training examples (17,511) use think_text (>200 chars)
- 7.8% of training examples (1,489) use short reason (<200 chars)
- Average assistant message: 843 chars (was 154 in V2)
- Max: 1,798 chars, estimated ~450 tokens max (fits in 1024 seq length)

### Rationale
V2's worst performance was on mid-range scores (moderate band MAE 2.77, high band MAE 2.56). The model would default to 8.5 when uncertain. By training on Opus's full reasoning, the model should internalize *why* articles score in specific ranges, not just memorize score patterns.

### Quality Concern: Agent vs Opus Think_text
| Source | Avg think_text length | Quality |
|--------|----------------------|---------|
| Opus (Batch API) | 1,133 chars | Genuine nuanced reasoning, considers profile deeply |
| Claude Code agents | 269 chars | Formulaic, template-based, keyword-matching style |

**Opus example:** *"Looking at this user's profile, they are a Computer Engineering student in Kuwait with strong interests in embedded systems and seeking their first engineering position. The article discusses Innovobot's acquisition of Voler Systems, a California-based engineering firm specializing in embedded systems, sensor integration, and IoT solutions..."*

**Agent example:** *"Article 'Innovobot Acquires Voler Systems' from The Indianapolis Star. Profile is a Marketing director at a consumer goods company in London, UK. The article's subject matter falls outside this profile's professional scope and personal interests."*

**Risk:** 14,684 agent-sourced examples (37.6% of total scores) may teach the model formulaic reasoning patterns rather than genuine analysis.

---

## 6. Other V2.1 Changes

### Chinese Parse Fix
Qwen3 sometimes outputs Chinese instead of English: `力评分：3.5 | 原因：...` instead of `SCORE: 3.5 | REASON: ...`. Added regex fallback in both `scorer_adaptive.py` and `evaluate_scorer.py`. Pre-training test: parse failures dropped from 5% to 2%.

### Keyword Pre-filter (Phase 1.5)
Before sending articles to LLM, checks keyword overlap between article text and profile interests. Zero keyword matches + low rule score → score as noise (skip LLM). Saves ~50-60% of LLM calls for obvious noise articles.

### Modelfile Fix
Removed `/nothink` directive that was causing 41% parse failures (model looped on slash-commands like `/soft`, `/nostart`). Now Qwen3 produces natural empty think blocks `<think>\n</think>` which get stripped.

### Inference Speed Fix (pending deployment)
Reduced `num_predict` from 2048 → 256 in `scorer_base.py`. The SCORE token comes first in model output, so we only need ~50-100 tokens. Prevents the model from generating verbose responses learned from think_text training data. Modelfile also needs updating from 2048 → 256.

---

## 7. Training/Inference Format Differences

| Element | Training | Inference | Concern Level |
|---------|----------|-----------|--------------|
| Category | Always `"general"` | Actual category (e.g., "AI & Technology") | MEDIUM — model never sees real categories |
| Keywords | Profile `interests` list | Category-specific keywords from `cat_items` | MEDIUM — different keyword sets |
| LANGUAGE line | Absent | Present ("Articles must be in English...") | LOW — extra instruction |
| Feedback text | Absent | Present (user corrections from memory) | LOW — extra context |
| Tracked fields format | Direct string from profile dict | Same fields via `_tracked_fields_block()` | NONE — produces identical output |
| Scoring rubric | Identical | Identical | NONE |
| Level note | Identical | Identical | NONE |
| Reply format | Identical | Identical | NONE |

---

## 8. Loss Curve (Current Training Run)

```
Step     Loss    Grad Norm   Learning Rate   Phase
─────────────────────────────────────────────────────
   5    16.01      1.197     6.667e-07       Warmup
  25    16.52      1.360     4.000e-06       Warmup
  65    12.60      0.683     1.000e-05       Peak LR
 105    10.10      0.590     9.963e-06       Early learning
 155     7.97      0.597     9.830e-06       Rapid descent
 205     6.26      0.617     9.603e-06
 255     4.80      0.799     9.288e-06
 305     5.60      0.849     8.889e-06       Plateau begins
 355     5.15      0.759     8.415e-06
 405     4.89      0.923     7.875e-06       Slow refinement
 455     4.71      0.951     7.280e-06       ← Latest logged
```

- **68.5% loss reduction** (16.01 → 4.71)
- Loss plateau around step 250-300 (~4.5-5.5), still slowly declining
- Gradient norms stable (0.5-1.0), no NaN or explosion
- Checkpoints saved at steps 200 and 400

**Note:** V2 training had much lower loss values (~2.2 starting). V2.1's higher loss is expected because think_text makes assistant messages ~5x longer, so cross-entropy accumulates over more tokens. This is a scale difference, not necessarily worse learning.

---

## 9. V2 Baseline (What We're Trying to Beat)

### Holdout Eval (1,500 samples — 50 completely unseen articles × 30 profiles)
```
MAE:                 1.533
RMSE:                2.102
Direction accuracy:  92.3%    (agrees on relevant ≥5 vs noise <5)
Spearman rho:        0.535
Within 1 point:      53.1%
Within 2 points:     80.6%
Parse failures:      108/1500 (7.2%)

Per-band MAE:
  noise (0-2.5):       1.39   (n=1129)
  tangential (2.5-4):  1.92   (n=168)
  moderate (4.5-6.5):  2.77   (n=47)   ← WORST — model struggles here
  high (6.5-8.5):      2.56   (n=40)   ← SECOND WORST
  critical (8.5-10):   1.19   (n=8)
```

### Contaminated Eval — through Ollama with broken format (500 samples)
```
MAE: 0.789, Direction: 98.5%, Spearman: 0.322, Parse failures: 6.2%
```
Note: This went through Ollama with `/nothink` and format drift. The original contaminated eval (direct PyTorch, correct format) was 0.393 MAE, 98.1% direction, 0.750 Spearman, 0 parse failures. The gap (0.393 → 0.789) shows format drift alone roughly doubles MAE.

### Target Metrics for V2.1
| Metric | V2 Holdout | V2.1 Target | Ideal |
|--------|-----------|-------------|-------|
| MAE | 1.533 | <1.2 | <0.8 |
| Direction accuracy | 92.3% | >95% | >97% |
| Spearman rho | 0.535 | >0.65 | >0.75 |
| Parse failures | 7.2% | <2% | 0% |
| Within 1pt | 53.1% | >60% | >70% |

---

## 10. Eval Infrastructure

- **`eval_v2.jsonl`** — 3,809 examples (contaminated: same articles as training, all profiles)
- **`eval_holdout_v2.jsonl`** — 1,500 examples (clean: 50 completely unseen articles × 30 profiles)
- **`holdout_articles.json`** — 50 article IDs reserved for holdout (never in training)
- **`evaluate_scorer.py`** — Automated eval script: scores eval set with Ollama model, computes MAE/RMSE/Spearman/direction/per-band/per-profile metrics

---

## 11. Findings from Cross-Claude Review

### Confirmed Facts
1. **V2's 0.393 MAE was direct PyTorch bf16 inference** — never went through Ollama/GGUF. The holdout 1.533 went through Ollama with broken Modelfile. These are incomparable measurements.
2. **V2 training data used short reasons** — verified: 172 chars avg, max 313 chars. V2.1's 843 chars avg is a 4.9x increase, not incremental.
3. **eval_v2.jsonl was split from same stage4 run** — identical format to training data. 0.393 tests memorization + format match, not generalization.
4. **V2 DID use dual weighting** — both WeightedRandomSampler AND per-sample loss weights. V2.1 preserves this correctly.
5. **Holdout is the metric that matters for shipping.** Contaminated metrics measure memorization, not production performance.
6. **V1 had CoT reasoning and got 1.553 MAE** — V2 improved via data scale (5,679→18,502 examples) and profile diversity (10→30), NOT via reasoning format. Think_text may not be the right lever for generalization.

### Open Risks
1. **Think_text might increase memorization** — longer reasoning repeats article-specific details in the training target, potentially teaching the model to pattern-match specific content rather than generalizable methods.
2. **Agent data (37.6% of scores) has formulaic reasoning** — 269 chars avg vs Opus 1,133 chars. May dilute training signal.
3. **813 articles may be the real bottleneck** — more diverse articles could matter more than richer reasoning on the same articles.
4. **The contaminated-holdout gap ratio matters** — V2: 0.393/1.533 = 3.9x reliance on memorization. If V2.1 has a smaller ratio (e.g. 1.0/1.2 = 1.2x), that indicates better generalization even if absolute contaminated MAE is worse.

### Consensus Recommendation
Let V2.1 finish. Evaluate on holdout. Also re-eval V2's original GGUF with corrected Modelfile on holdout to get a fair baseline. If V2.1 beats corrected-V2 on holdout, ship it. If not, prioritize expanding article diversity (813→2000+) over further format changes.

## 12. Original Questions (pre-review)

1. **Is training on think_text the right approach?** We're teaching the model to output verbose reasoning in hopes it internalizes better scoring logic. But at inference we cap output at 256 tokens and only parse the SCORE. Is the model learning useful representations, or just learning to be verbose?

2. **Agent data quality contamination:** 37.6% of total scores come from agents with formulaic 269-char think_text (vs Opus's 1,133-char genuine reasoning). Are these helping or hurting? Should we filter them out or weight them lower?

3. **90.8% noise in training data** — Even with weighted sampling (noise at 0.5x, critical at 3.0x), the model sees overwhelming majority 0-2 scores. Only 101 critical examples (0.5%). Is this enough signal for high-relevance scoring?

4. **Category always "general" in training** — At inference, the model sees actual categories like "AI & Technology" but was trained with Category: general for every example. Does this create a distribution shift?

5. **1 epoch, 1e-5 LR, cosine decay** — Loss plateaued around step 250 at ~5.0. Would a second epoch help? Higher LR? Different schedule?

6. **Loss values seem high** — Starting at 16.0, currently at 4.7. V2 started at ~2.2. Is this purely the longer assistant messages, or a sign of something wrong?

7. **Training on V1-merged base** — We're fine-tuning a model that already has V1 DoRA adapters merged. Is stacking fine-tunes like this risky for forgetting or instability?

8. **num_predict 256 at inference** — Will the model's learned verbosity cause it to "waste" tokens on preamble before SCORE, potentially getting truncated before outputting the score?

9. **Format misalignment** — Training uses `Category: general` and `Keywords: {profile interests}`, but inference uses actual category labels and category-specific keywords. Is this significant?

10. **Score distribution skew** — Average training score is 1.26. The model might develop a strong prior toward low scores. Is the dual weighting system sufficient to counteract this?

---

## 12. Post-Training Plan

1. Phase 3 auto-runs after training: merge DoRA adapters → export GGUF → sanity eval
2. Update Modelfile `num_predict` from 2048 → 256
3. Register: `ollama create stratos-scorer-v2 -f Modelfile`
4. Start Ollama: `sudo systemctl start ollama`
5. Evaluate on holdout: `python3 evaluate_scorer.py --eval-file data/v2_pipeline/eval_holdout_v2.jsonl`
6. Compare against V2 baselines
7. Commit results

---

## 13. File Locations

```
Training script:     backend/data/v2_pipeline/train_v2.py
Training data:       backend/data/v2_pipeline/training_v2.jsonl (44MB, 19K examples)
Scores database:     backend/data/v2_pipeline/scores_v2.json (39,071 scores)
Articles:            backend/data/v2_pipeline/articles_v2.json (813 articles)
Base model:          backend/data/v2_pipeline/v1_merged_base/
Checkpoints:         backend/data/v2_pipeline/training_output/checkpoint-{200,400,...}/
Holdout eval:        backend/data/v2_pipeline/eval_holdout_v2.jsonl (1,500 examples)
Scorer (inference):  backend/processors/scorer_adaptive.py
Scorer (base):       backend/processors/scorer_base.py
Eval script:         backend/evaluate_scorer.py
Modelfile:           backend/data/v2_pipeline/Modelfile
Pre-training backup: backend/data/v2_pipeline/v3_pre_training_backup/
Training log:        backend/data/v2_pipeline/training_output/v2.1_training_run.log
Profile definitions: backend/data/v2_pipeline/profiles_v2.py (47 profiles)
```
